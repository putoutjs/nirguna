export const compare2 = (eax) => {
    if (eax === 10)
        return 5;
    
    return 10;
};

if (dx === 1)
    dh = 1;
else
    dh = 0;


if ([sec_number] > 0x12) {
    inc([track_number]);
    [sec_number] = 1;
} else {
    al = 0;
}