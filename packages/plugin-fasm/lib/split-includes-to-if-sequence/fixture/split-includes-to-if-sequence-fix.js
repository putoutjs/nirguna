if ([].includes())
    x();

if ([a].includes())
    x();

if (notDeclared.includes(z))
    x();

const notArray = getArray();

if (notArray.includes())
    x();

{
    if (al === LEFT_ALT)
        jmp(again);
    
    if (al === LEFT_ALT_UP)
        jmp(again);
    
    if (al === LEFT_CTRL)
        jmp(again);
    
    if (al === LEFT_CTRL_UP)
        jmp(again);
}
