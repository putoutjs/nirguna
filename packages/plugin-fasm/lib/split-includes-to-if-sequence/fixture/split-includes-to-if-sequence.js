const ignored = [
    LEFT_ALT,
    LEFT_ALT_UP,
    LEFT_CTRL,
    LEFT_CTRL_UP,
];

if ([].includes())
    x();

if ([a].includes())
    x();

if (notDeclared.includes(z))
    x();

const notArray = getArray();

if (notArray.includes())
    x();

if (ignored.includes(al))
    jmp(again);
